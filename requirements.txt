# ZhiSync SDK — core uses only Python stdlib (no mandatory deps)

# Simulation framework dependencies (needed to run simulation/ and reproduce paper results)
torch>=2.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
tqdm>=4.65
psutil>=5.9
librosa>=0.10
soundfile>=0.12
scikit-learn>=1.3
